# audit — Data Relations
# 20 contracts · 5 contexts · bronze

domain: audit
context: audit_aggregated

relations:
  - ref_name: "cross_audit_audit_curated"
    ref: "bronze_audit_audit_aggregated_013.ID > audit:audit_curated:bronze_audit_audit_curated_017.ID"

  - ref_name: "cross_audit_audit_logs"
    ref: "bronze_audit_audit_aggregated_013.ID > audit:audit_logs:bronze_audit_audit_logs_001.ID"

  - ref_name: "cross_audit_audit_profile"
    ref: "bronze_audit_audit_aggregated_013.ID > audit:audit_profile:bronze_audit_audit_profile_009.ID"

  - ref_name: "cross_audit_audit_snapshot"
    ref: "bronze_audit_audit_aggregated_013.ID > audit:audit_snapshot:bronze_audit_audit_snapshot_005.ID"

